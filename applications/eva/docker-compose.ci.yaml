version: '3.9'
services:
  eva:
    image: $CONTAINER_REGISTRY/eva/dev:$BUILDKITE_COMMIT
    entrypoint: ""
    command: /bin/sh -c "/app/applications/eva/image.binary db migrate keyspace && /app/applications/eva/image.binary db seed && /app/applications/eva/image.binary"
    env_file: ../../applications/eva/.env.ci
    depends_on:
      - elasticsearch
      - temporal
    ports:
      - 3020:8000
      - 3030:8080
    deploy:
      restart_policy:
        condition: on-failure
        max_attempts: 20

  run:
    depends_on:
      - eva