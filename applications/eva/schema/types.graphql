type Cookie {
  sameSession: Boolean!
  registered: Boolean!
  redeemed: Boolean!
  session: String!
  email: String!
  invalid: Boolean!
}

type User @key(fields: "id") {
  id: String!
  username: String!
}

type Authentication {
  cookie: Cookie
  user: User
}