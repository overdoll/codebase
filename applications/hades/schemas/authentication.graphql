type Cookie {
    sameSession: Boolean!
    registered: Boolean!
    redeemed: Boolean!
}
type User {
    username: String!
}

type Authentication {
    cookie: Cookie
    user: User
}

type AuthListener {
    authorized: Boolean!
    redirect: Boolean!
    cookie: Cookie
}