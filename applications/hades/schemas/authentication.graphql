type Authentication {
    success: Boolean!
}

type AccountData {
    registered: Boolean!
    sameSession: Boolean!
}

type JoinState {
    existingUser: Boolean!
}

type Registration {
    username: String!
}

type AuthenticationState {
    authorized: Boolean!
    registered: Boolean!
}

type SameSession {
    same: Boolean!
}