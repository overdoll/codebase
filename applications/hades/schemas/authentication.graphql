type Authentication {
    success: Boolean!
}

type AccountData {
    registered: Boolean!
}

type JoinState {
    existingUser: Boolean!
}

type Registration {
    username: String!
}

type AuthenticationState {
    Redeemed: Boolean!
}

type SameSession {
    same: Boolean!
}