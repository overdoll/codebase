type Query {
  users(id: String, username: String, email: String): [User!]
}

type User {
  id: String!
  username: String!
  email: String!
}

input UserInput {
  id: String
  username: String
  email: String
}
