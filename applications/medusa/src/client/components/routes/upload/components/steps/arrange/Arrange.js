/**
 * @flow
 */
import type { Node } from 'react';

type Props = {
  uppy: any,
  onAddFiles: any,
  onRemoveFile: any,
  files: any,
  onArrangeFile: any,
};

export default function Arrange({
  uppy,
  onAddFiles,
  onRemoveFile,
  onArrangeFile,
}: Props): Node {
  return null;
}
