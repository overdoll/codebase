/**
 * @flow
 */
import type { Node } from 'react';

type Props = {
  uppy: any,
  dispatch: any,
  state: any,
};

export default function Tag({ uppy, state, dispatch }: Props): Node {
  return <>tag file</>;
}
