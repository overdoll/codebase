/**
 * @flow
 */

import { useTranslation } from 'react-i18next'

export default function RecoveryCodes (props: Props): Node {
  const [t] = useTranslation('settings')

  return (
    <>
      recovery codes
    </>
  )
}
