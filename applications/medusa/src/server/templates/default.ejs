<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
    <link rel="shortcut icon" href="<%= favicon %>">
    <link rel="manifest" href="<%= manifest %>">
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=2,shrink-to-fit=no" />
    <meta name="csrf-token" content="<%= csrfToken %>">
    <meta name="nonce" content="<%= nonce %>">
    <meta name="browser-language" content="<%= i18nextLang %>">
    <title><%= title %></title>
    <%- preload %>
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400">
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css?family=Nunito:700">
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400" rel="stylesheet" type="text/css">
    <link href='https://fonts.googleapis.com/css?family=Nunito:700' rel='stylesheet' type="text/css">
    <%- styles %>
    <style nonce="<%= nonce %>" data-emotion="css <%= emotionIds %>"><%= emotionCss %></style>
    <script nonce="<%= nonce %>" type="application/json" id="relay-store"><%- relayStore %></script>
    <script nonce="<%= nonce %>" type="application/json" id="i18next-store"><%- i18nextStore %></script>
    <script nonce="<%= nonce %>" type="application/json" id="flash-store"><%- flashStore %></script>
</head>
<body>
<div id="root"><%- html %></div>
<%- scripts %>
</body>
</html>