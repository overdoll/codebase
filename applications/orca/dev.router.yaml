server:
  cors:
    allow_any_origin: false
    origins:
      - https://studio.apollographql.com
      - https://nikita.local-sandbox.overdoll.dev
      - https://ilya.local-sandbox.overdoll.dev
      - https://ci.overdoll.dev
    allow_credentials: true
    allow_headers: [ Content-Type, Authorization ]
    methods: [ POST, OPTIONS ]
    expose_headers: [ ]

  listen: 0.0.0.0:8000

plugins:
  overdoll.passport:
  overdoll.relay_persisted_queries:
