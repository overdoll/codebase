cors:
  allow_headers:
    - Content-Type
    - Authorization
    - X-overdoll-Security

  origins:
    - https://overdoll.test
    - https://overdoll.com
    - https://studio.apollographql.com

  allow_credentials: true

  methods:
    - GET
    - POST
    - OPTIONS

csrf:
  unsafe_disabled: true

plugins:
  overdoll.passport:
  overdoll.persisted_queries:
  overdoll.relay:
