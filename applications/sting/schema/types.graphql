type CharacterRequestType {
  name: String!
  media: String!
}

type Media {
  id: String!
  thumbnail: String!
  title: String!
}

type Artist {
  id: String!
  avatar: String!
  username: String!
}

type Character {
  id: String!
  thumbnail: String!
  name: String!
  media: Media!
}

type Category {
  id: String!
  thumbnail: String!
  title: String!
}

type Validation {
  code: String!
}

type Response {
  validation: Validation
  ok: Boolean!
}

scalar Time

interface Node {
  id: ID!
}