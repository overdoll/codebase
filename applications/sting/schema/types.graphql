type CharacterRequestType {
  name: String!
  media: String!
}

type Media {
  id: ID!
  thumbnail: String!
  title: String!
}

type Artist {
  id: ID!
  avatar: String!
  username: String!
}

type Character {
  id: ID!
  thumbnail: String!
  name: String!
  media: Media!
}

type Category {
  id: ID!
  thumbnail: String!
  title: String!
}